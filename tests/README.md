# Tests

This directory is reserved for cross-workspace testing organization.

Initial structure:

- `unit/` for shared unit test conventions
- `integration/` for service and boundary-level integration tests
- `fixtures/` for reusable test data and support assets
