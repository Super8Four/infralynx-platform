# Deploy

This directory is reserved for application deployment-facing assets that belong with the platform repository rather than the infrastructure repository.

Examples:

- container build context notes
- runtime environment templates
- service packaging assets
